server {
	listen              443 ssl;
	server_name         consomalin.ovh;

	ssl_certificate     /app/ssl/nginx/consomalin.ovh/cert.pem;
	ssl_certificate_key /app/ssl/nginx/consomalin.ovh/privkey.pem;

	return 301 http://consomalin.shop$request_uri;
}
